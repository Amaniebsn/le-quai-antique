
{% extends 'base.html.twig' %}

{% block title %}Connexion! - Le Quai Antique{% endblock %}

{% block body %}
<div class="block->small">
    {% if error %}
        <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
    {% endif %}

    {% if app.user %}
        <div class="mb-3">
            Vous êtes connecter {{ app.user.userIdentifier }}, <a href="{{ path('app_logout') }}">Deconexion</a>
        </div>
    {% endif %}
     <form action="{{ path('app_login') }}" method="post">
        <label for="username">E-mail:</label>
        <input type="text" id="username" name="_username" value="{{ last_username }}" class="form-control" placeholder="Votre adresse e-mail"required autofocus>>

        <label for="password">Mot de passe:</label>
        <input type="password" id="password" name="_password" class="form-control" placeholder="Votre adresse mot de passe"required>

        {# If you want to control the URL the user is redirected to on success
        <input type="hidden" name="_target_path" value="/account"> #}

        <button class="btn btn-lg btn-light btn-sm" type="submit">Se connecter</button>
    </form>
</div>
{% endblock %}