{% extends 'base.html.twig' %}

{% block title %}Nos menus - Le Quai Antique{% endblock %}

{% block body %}
<div class="text-center" >
        <div class="title-2">
            <div class="col"><img src="{{ asset('assets/pictures/Home.png') }}" width="1450" height="400"/></div>
        </div>
        </div>      
    </div>
    <hr>
    
<div class="titre">
<h1 class="text-center">NOS MENUS</h1>
</div>
<hr>

<div class="row">
   {% for menus in menus %}
      <div class="col-md-4"></div>
      <div class="menus-item text-center">
      <div class="titre">
         <h4> {{menus.name}}</h4>
      </div>
          <span> {{menus.description}}</span>
          <span class="menus-price">{{ ( menus.price / 100 ) | number_format(2, ',', '.')}} €</span>      
    </div>
</div>
   {% endfor %}
   
{% endblock %}
