{% extends 'base.html.twig' %}

{% block title %}{{ menus.name }} Le Quai Antique {% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

 <div class="row">
   {% for menus in menus %}
      <div class="col-md-4"></div>
      <div class="menus-item text-center">
          <img src="{{ asset('assets/pictures/' ~ menus.illustration) }}" height="100px" widht="100px" class="img-fluid" alt="">
          <h4> {{menus.name}}</h4>
          <span> {{menus.description}}</span>
          <span class="menus-price">{{ ( menus.price / 100 ) | number_format(2, ',', '.')}} €</span>           
      </div>
</div>
{% endblock %}